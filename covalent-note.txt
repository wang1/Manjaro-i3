  sudo pacman -S nodejs；
  sudo npm i -g yarn;
 sudo  yarn config set registry https://registry.npm.taobao.org --global
  sudo yarn global add @angular/cli
  sudo ng set --global packageManager=yarn
  ng new covalentTest
  cd covalentTest

#install angular material and covalent module
  yarn add @angular/material @angular/cdk @angular/animations @covalent/core

#build own material module file, import own needed modules
  ng g m my-own-material --module=app --flat

#copy following content into my-own-material.module.ts file
  import { NgModule } from '@angular/core';
  import { MatButtonModule, MatCheckboxModule } from '@angular/material';
  import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
  import { CovalentLayoutModule } from '@covalent/core/layout';

  @NgModule({
    imports: [MatButtonModule, MatCheckboxModule, BrowserAnimationsModule, CovalentLayoutModule],
    exports: [MatButtonModule, MatCheckboxModule, BrowserAnimationsModule, CovalentLayoutModule],
  })
  export class MyOwnMaterialModule { }


#Including a theme is required to apply all of the core and theme styles to your application.
To get started with a prebuilt theme, include one of Angular Material's prebuilt themes globally in your application. If you're using the Angular CLI, you can add this to your styles.css:
  @import "~@angular/material/prebuilt-themes/indigo-pink.css";
------------------------------------------------------------------------

#now, it's ok copy the following into app.component.html file for testing basic material component
  <div class="button-row">
    <button mat-raised-button>Basic</button>
    <button mat-raised-button color="primary">Primary</button>
    <button mat-raised-button color="accent">Accent</button>
    <button mat-raised-button color="warn">Warn</button>
    <button mat-raised-button disabled>Disabled</button>
    <a mat-raised-button routerLink=".">Link</a>
  </div>

#then, copy the following into app.component.html file for testing covalent component
